# Expo / frontend — copy to `.env` in this same folder (`app/`)
# Restart `npm run web` (or `expo start`) after changing.

# Backend API base URL (no trailing slash)
EXPO_PUBLIC_API_URL=http://localhost:8080

# Production example:
# Azure Container Apps production backend:
# EXPO_PUBLIC_API_URL=https://smartfinance-api.wittyglacier-7789888f.eastus2.azurecontainerapps.io
